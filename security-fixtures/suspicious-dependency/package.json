{
  "name": "left-pad-helper",
  "version": "1.0.7",
  "description": "INERT TEST FIXTURE — typosquat-style name with malicious lifecycle hooks. Not a real dependency; not installed by npm.",
  "main": "index.js",
  "scripts": {
    "preinstall": "node install.js",
    "postinstall": "node install.js"
  },
  "_comment": "Signature: install-time lifecycle hooks executing a script. This package is intentionally NOT referenced anywhere, so npm never runs it."
}
